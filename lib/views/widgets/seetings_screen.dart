import 'package:flutter/material.dart';

class SeetingsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('SeetingsScreen'),
      ),
    );
  }
}
